/** Step 3 — target org(s) configuration (PLAN §5, CHECKLIST §9). */
export default function Step3TargetOrgs() {
  return (
    <section>
      <h2>3. Target orgs</h2>
      <p>Add one or more orgs to load data into.</p>
      {/* TODO: dynamic org list with add/remove/test */}
    </section>
  );
}
