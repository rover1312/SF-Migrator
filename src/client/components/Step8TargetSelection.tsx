/** Step 8 — choose targets + loading strategy (PLAN §5, CHECKLIST §15). */
export default function Step8TargetSelection() {
  return (
    <section>
      <h2>8. Targets for loading</h2>
      <p>Pick which target orgs receive data and set insert/update/upsert mode.</p>
    </section>
  );
}
