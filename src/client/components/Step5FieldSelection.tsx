/** Step 5 — field selection with CRUD color coding (PLAN §5, CHECKLIST §11). */
export default function Step5FieldSelection() {
  return (
    <section>
      <h2>5. Fields & permissions</h2>
      <p>Green = allowed, red = denied, gray = N/A.</p>
      {/* TODO: per-object field tables + select-all */}
    </section>
  );
}
