/** Step 1 — choose New migration or Import configuration (PLAN §5, CHECKLIST §7). */
export default function Step1ConfigMode() {
  return (
    <section>
      <h2>1. Configuration mode</h2>
      <p>Start a new migration or import a saved JSON/YAML config.</p>
      {/* TODO: file upload + preview + validation */}
    </section>
  );
}
