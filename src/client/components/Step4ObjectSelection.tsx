/** Step 4 — object selection (PLAN §5, CHECKLIST §10). */
export default function Step4ObjectSelection() {
  return (
    <section>
      <h2>4. Objects</h2>
      <p>Search and select SObjects to migrate. Order matters for dependencies.</p>
      {/* TODO: virtualized list + search + record counts */}
    </section>
  );
}
