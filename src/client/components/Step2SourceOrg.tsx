/** Step 2 — source org authentication (PLAN §5, CHECKLIST §8). */
export default function Step2SourceOrg() {
  return (
    <section>
      <h2>2. Source org</h2>
      <p>Connect the org to extract from (OAuth or username/password).</p>
      {/* TODO: connection form + status indicator */}
    </section>
  );
}
