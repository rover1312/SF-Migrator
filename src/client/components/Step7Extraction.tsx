/** Step 7 — extraction settings + progress (PLAN §5, CHECKLIST §13). */
export default function Step7Extraction() {
  return (
    <section>
      <h2>7. Extraction</h2>
      <p>Extract to local CSV/JSON in data/extracted/ with progress + retry.</p>
      {/* TODO: settings form + progress dashboard */}
    </section>
  );
}
