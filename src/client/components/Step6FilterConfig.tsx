/** Step 6 — filter builder producing SOQL WHERE (PLAN §5, CHECKLIST §12). */
export default function Step6FilterConfig() {
  return (
    <section>
      <h2>6. Filters</h2>
      <p>Build per-object WHERE clauses with live SOQL preview.</p>
      {/* TODO: filter rows + SOQL preview + test-count */}
    </section>
  );
}
