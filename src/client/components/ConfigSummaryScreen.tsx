/** Pre-extraction summary: review everything, export config, confirm. */
export default function ConfigSummaryScreen() {
  return (
    <section>
      <h2>Summary</h2>
      <p>Review source, targets, objects, fields, filters, and settings.</p>
      {/* TODO: collapsible sections + export + confirm */}
    </section>
  );
}
